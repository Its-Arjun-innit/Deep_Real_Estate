# === OS generated files ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
.thumbnail

# === IDE / Editor ===
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# === Node (if added later) ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# === Uploads / duplicates ===
# This folder mirrors Elements/ and REDESIGN-SPEC.md — not needed in version control
uploads/

# === Scraps / rough design screenshots ===
scraps/

# === CorelDRAW source files (large binary, not web-usable) ===
Elements/deep.cdr

# === Large source footage (181MB) — the web loop assets/hero.mp4 is used instead ===
Elements/Video Project 2.mp4

# === Build / cache artifacts ===
dist/
.cache/
*.log
